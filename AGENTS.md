# MHRise Monster Coach 项目约束

## 支持范围

- 目标运行时：Steam《怪物猎人崛起：曙光》16.0.2.0、REFramework `mhrise / TDB 71`。
- 首个怪物包：普通大师等级轰龙；联机、怪异任务和未知运行时保持只读。
- 不修改存档、正式任务奖励、怪异调查或联机数据。

## 架构边界

- 保持 MVC：`model.lua` 管理领域状态，`view.lua` 只渲染，`controller.lua` 编排输入与用例。
- REFramework 和游戏对象访问集中在 `runtime.lua` 与 `action_reader.lua`。
- 怪物知识集中在 Profile 与校准 JSON；不得把新怪物 Action ID 散布到业务代码。
- 只有第二个真实怪物包落地后才提取 Profile Registry，不提前建立通用插件框架。

## 安全门禁

- 发生过原生 `c0000005` 崩溃；`diagnostic_safe_mode` 必须默认开启，直到每项能力按只读识别、时间控制、手动重置、生命写入顺序分别通过实机验收。
- 禁止自动枚举怪物类型的全部方法或调用未知 Action/FSM 方法。
- 新的写操作必须具备单人、支持版本、任务状态和能力开关四重门禁，并提供退出/异常恢复。
- 自定义训练任务使用独立 Quest ID、零实质奖励和单怪配置；不得覆盖现有任务文件。

## 验证与发布

- 每次改动运行 `tests/` 下全部 Lua 测试，并静态解析所有 Lua/JSON。
- 自动测试、已安装文件哈希、运行时日志和真实游戏验收必须分别报告。
- 安装或打包只从当前 Git 工作树生成；不得把游戏目录中的用户配置、日志、Dump 或其他 Mod 反向复制进仓库。

## 逆向与资料检索

- 进入实质功能开发或遇到卡点时，先加载并遵循全局 `$feature-development-and-problem-solving` Skill；本节只定义本项目的资料优先级和安全补充规则。
- 遇到问题时先从全局视角建立问题模型：明确最终目标、正常玩家的操作逻辑、可验证证据和长期可维护的代码边界，避免直接陷入局部试错。
- 遇到地图、任务、怪物或游戏数据问题时，优先检查 `wwylele/mhrice` 的公开数据与提取实现，再决定是否增加运行时探针。
- 没有可复用方案时，设计有边界、可证伪且能集中收集证据的本地实验；同一路径默认 2–4 次，并优先合并为安全的批量自动验证，不做无休止猜测。
- 本地实验仍未解决时，检索社区现有 Mod、开源代码、逆向资料或游戏原生机制，优先复用经过验证的成熟实现。
- 得到结果后再次从全局视角复盘：确认方案是否真正符合玩家直觉、能够自动化、支持后续怪物和高级功能，并收敛为清晰接口；必要时调整整体设计，不在错误路线之上继续打补丁。
- 公开静态数据与运行时证据分别标注来源；公开数据缺少关键运行时对象时，采用静态坐标系与原生事件交叉验证，不把推测写成确定结论。
