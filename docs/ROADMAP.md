# 怪物陪练开发路线图

本文件记录完整目标、当前状态和解锁门禁。功能不会因为当前不可用而被删除；未通过安全验证的能力保持锁定，并显示具体原因。

## 状态定义

- `已验证`：自动测试和对应实机门禁均通过。
- `候选`：代码已实现、自动测试通过，仍需一次集中实机验收。
- `开发中`：契约或实现正在推进，不对用户宣称可用。
- `待解锁`：设计保留，但依赖安全写入、完整数据或前置功能。
- `不开发`：明确保留在路线图中，但不进入当前或可预见批次；只有重新评估条件满足后才能改回候选。

## 核心路线

| 顺序 | 功能 | 状态 | 当前结果 | 下一门禁 |
|---:|---|---|---|---|
| 1 | 专用轰龙陪练任务 | 候选 | MR 4★、塔之秘境、单只普通轰龙可进入 | 连续进退、零奖励及列表排序验收 |
| 2 | 实时 Action/Motion 解析 | 已验证 | 读取 `ActionCategory:ActionNo`、Motion 名称、当前帧和总帧 | 持续兼容性测试 |
| 3 | 招式名称与固定/条件派生 | 候选 | 中文招式名、结构化威胁方向、处理方向和已验证固定边已接入提示；未知项明确降级 | 继续补全条件边与随机权重 |
| 4 | 前摇/有效判定/收招阶段 | 候选 | Native Provider 自动关联 Action/Motion/帧并持久化多段窗口；三次稳定观察自动确认 | 一次集中实机验收自动记录与阶段提示 |
| 5 | 武器上下文应对提示 | 可选扩展（默认关闭） | 已确认阶段、判定倒计时、多段间隙保护与太刀红蓝书/资源/可取消状态已组合为实时建议；关闭时不计算或显示武器建议 | 不阻塞轰龙 MVP；核心闭环稳定后再核对提示时机与不同交换技配置 |
| 6 | 子弹时间 | 候选（核心路径已实机验证） | F6 单击切换 0.25x/1.0x 已实机通过；使用 via.Application GlobalSpeed，菜单/异常/退出具备自动恢复 | 补验菜单、任务退出和脚本重载恢复后升级为已验证 |
| 7 | 锚点记录 | 实机候选 | 自动默认锚点和 F8 手动覆盖已实现；锚点改为独立复制的位置/旋转值 | 拆分猎人与怪物锚点，并增加距离预设 |
| 8 | 直接局内重置 | 开发中 | 旧版直接写回猎人 Transform 会触发原生 `c0000005`，该路径保持禁用；已加入目标类型只读元数据探针 | 从 Enemy/Player/Stage/Quest 管理器中筛选原生重建、重生或安全 Warp 入口，再做受控调用 |
| 9 | 一键重开任务 | 已验证 | F7 一次启动：原生 `notifyReset()` 返回据点，再通过固定 Quest Identifier 创建原生任务会话并自动出发；自动启动、首次接取、进入任务、F7 重开和再次进入已在同一会话完整通过 | 缩短据点就绪后的保守延迟，并回归一次快速路径 |
| 10 | 指定怪物出招/派生 | 核心批次已验证 | 同一游戏、同一任务自动完成 8/8 精选起手；5 个独立招式、2 个固定派生和 1 个条件派生合同全部有效，短/长距钩咬均命中 Action 5001，直线冲锋命中 Action 15 漂移再冲锋 | 集中核对用户 Overlay 的派生树、状态和错误提示可读性；之后再选择 2–4 个差异化高价值起手，不按 Action 数量堆菜单 |
| 11 | 手柄操作 | 候选且默认关闭 | 已实机读取闪避、X、A 的手柄绑定；武器特殊键在 `pad_pl_Conf` 中无独立条目，不猜组合键 | 将已解析按钮接入冲突检测；缺失或组合映射保持安全拦截 |
| 12 | 训练结果分类 | 候选 | 已区分明确受击、生命追踪无伤、通用防御/回避状态、武器动作候选和未分类；核心结果不要求自动执行玩家技能 | 集中验收受击、未受击与通用回避状态；武器技能成功语义留给可选扩展 |
| 13 | 判定与输入事件时间轴 | 实测布局已验证 | 独立招式与条件派生两份真实报告均通过共享布局分析：中文字体、内容矩形、逐行宽度、无溢出、核心文案与可选 Response 隔离有效 | 视觉层级只保留一次画面确认；继续补充通用结果语义，不重测布局几何 |
| 14 | 更多武器应对 | 可选扩展 | 太刀上下文规则和数据契约已建立 | 核心闭环稳定后按武器数据包扩展，不把规则写死在通用 Controller |
| 15 | 多怪物数据包 | 待解锁 | 通用 Runtime/Model 与轰龙数据包已分离 | 轰龙闭环稳定后，用第二只复杂怪物验证扩展点 |
| 16 | 仅怪物减速实验模式 | 不开发 | 有助于观察招式，但会扭曲实战速度比例，且必须同步动画、AI、位移、碰撞、投射物与派生计时 | 不排期；仅当全局子弹时间和事件时间轴仍无法满足招式拆解需求，且存在完整同步方案时重新评估 |
| 17 | 真正的视频或世界状态回放 | 不开发 | 视频录制不能提供可交互状态；完整世界快照缺少安全原生机制，既有 Transform 写回已证明会崩溃 | 不排期；事件时间轴作为正式替代。只有引擎原生 Replay/SaveState 机制被证明安全、完整且可隔离存档时重新评估 |

## 推荐实施顺序

1. 完成轰龙实用版：覆盖最值得练习的 8–12 个起手及主要固定、条件和随机派生。
2. 完成通用训练结果分类与判定时间轴：受击、未受击、通用防御/回避状态和相对判定帧。
3. 在轰龙闭环稳定后扩展第二只复杂怪物，验证多怪物架构。
4. 作为独立可选批次，继续太刀技能建议并按武器数据包扩展见切、居合、反击等专属语义。

“仅怪物减速实验模式”和“真正的视频或世界状态回放”不属于上述实施序列，不得在核心闭环完成前消耗开发与验收预算。

## 当前推进批次

当前源码候选版本为 `0.49.49-overlay-probe-surface`。第 5 次有界实机运行已完成 8/8 动作/派生合同且覆盖缺口为 0；重复 `D3D12Core.dll / c0000409` 退出仍是独立稳定性阻塞。Overlay 的共享实测布局现已完成两份互补实机证据：右回旋训练完整结束，2560×1440 下 14 行、720×290、中文样本 53 px、横纵溢出均为 false；直线冲锋本轮未进入已收录后继，业务训练诚实失败，但 15 行条件派生界面独立通过并真实显示 `Next (condition)`。两轮都隐藏可选武器 Response，用户原 `overlay_enabled=false` 在报告后恢复，游戏随后持续响应且没有新增应用错误。下一步不再重测布局，而是解决外部渲染稳定性变量并保留一次视觉层级确认。

本次范围决定遵循最小扩展边界：不删除 `response_long_sword.lua`、武器知识数据或开发验收器，不提前建立武器插件注册表；只用一个默认关闭的配置值控制 Model 计算与 View 呈现。通用时间轴不依赖该开关，因此以后重新启用太刀或增加第二种武器时无需改写怪物训练闭环。

`0.49.44` 没有再实现一套游戏内批量状态机，而是复用已通过实机门禁的 `run_probe_session.ps1` 作为安全工作单元。批处理计划同时与轰龙数据包的场景 ID、训练类别、验证状态和最大重复次数交叉校验，避免文档计划漂移成未授权 Action。批次失败不会自动重试或重启任务；F7 仍只作为单场景执行器既有的原生恢复能力，不被聚合层滥用为掩盖失败的循环。

`0.49.29` 已在真实游戏中完成四项精确 `isDelay(CommandButton2)` 调用：`Atk_X=0`、`Atk_A=1`、`Atk_R_A=41`、`Escape=3` 均成功返回 Boolean，调用 4/4、失败 0、玩法写入 0，游戏继续正常响应。该结果只证明当前玩家实例、命令枚举和调用约定有效，不解释为动作成功；完整证据见 `docs/evidence/PLAYER_INPUT_READ_ACCEPTANCE_2026-08-29.json`。

`0.49.30` 首次引入真实语义输入写入，但只存在于自动开发探针：仅允许受支持的离线普通陪练任务和 `Escape`，只在一次 `UpdateHID` 后对 `getTrg()` 返回位集调用一次继承的 `set(UInt32)`，不调用 `clear()` 或 `setDatas()`，也不循环维持按下。下一次 HID 更新必须由游戏自然清除该触发位，并在最多 3 个 HID 周期内由 `isTrg(Escape)` 严格确认 Boolean `false`；注入前取消、签名变化、非 Boolean 返回、未释放、联机、错误任务或不支持版本全部失败关闭。此实验不接入正常陪练 UI、快捷键或武器连招，实机报告必须同时证明写入恰好 1 次、自然释放和游戏继续响应，才允许下一版把范围扩大到一个武器命令。

`0.49.30` 实机已经通过写入与释放门禁：`Escape=3` 只写入 1 次，在第 2 个 HID 周期自然释放，游戏持续响应。但触发前后猎人均处于古塔到达动画，未出现回避节点；因此命令范围保持不变。`0.49.31` 增加 15 帧已验证中立节点门禁，并把动作验收收紧为 `atk.esc_*`，任意节点变化不再触发下一命令解锁。完整证据见 `docs/evidence/SEMANTIC_TRIGGER_LIFECYCLE_ACCEPTANCE_2026-08-29.json`。

`0.49.31` 的实机复测在 `wait.main` 连续稳定 15 帧后才注入，但 60 帧内仍无任何动作节点变化；单写入、自然释放和游戏稳定性继续通过。Manager `getTrg()` 位集路线由此停止，不再换帧位重复猜测。`0.49.32` 与 `0.49.33` 排除两个 GameObject 位置，`0.49.34` 完成真实运行实例的只读捕获门禁，`0.49.35` 证明 UpdateHID 末尾的成对生命周期安全但过晚，`0.49.36` 又证明原生 update hook 内的同一成对窗口仍不被动作层消费。至此托管语义写入入口正式关闭；证据链见 `docs/evidence/ACTIONABLE_TRIGGER_ACCEPTANCE_2026-08-29.json`、`docs/evidence/PLAYER_GAMEOBJECT_STM_INPUT_REJECTION_2026-08-29.json`、`docs/evidence/MANAGER_GAMEOBJECT_STM_INPUT_REJECTION_2026-08-29.json`、`docs/evidence/STM_PLAYER_INPUT_HOOK_CAPTURE_ACCEPTANCE_2026-08-29.json`、`docs/evidence/STM_PLAYER_INPUT_PAIRED_TRIGGER_2026-08-30.json`、`docs/evidence/STM_PLAYER_INPUT_HOOK_WINDOW_REJECTION_2026-08-30.json`。

`0.41.0` 将指定出招菜单收敛为“精选起手目录”，按独立关键招式、固定派生起手和条件派生起手分组。每个入口仍必须先展示派生树，再开放开始按钮；训练顺序、分类和简介全部属于怪物数据包，通用 Controller 不写死轰龙招式。当前只公开已具备安全证据的咆哮和短距半回转钩咬，后续起手通过同一数据契约批量加入，不把未经验证的 Action 暴露给玩家。

`0.41.1` 将目录分组、执行模式、简介和三层派生树纳入只读运行时快照。开发验收器可以在标题阶段一帧完成产品目录检查，不再要求玩家打开 REFramework 菜单逐项确认。

`0.41.2` 修复自动距离调查遗漏猎人死亡生命周期的问题。调查一旦离开战斗层便立即停止输入与采样，进入准备区恢复状态；外部导航器重新通过原生传送点进入战斗区后，才继续同一批调查。候选汇总同时过滤跨层样本和非 Phase00 事件，防止把死亡回城后的巨大距离写成怪物起手条件。

`0.41.3` 在死亡恢复重新进入战斗层的同一帧发布恢复确认，确保外部导航器立即释放传送锁存，不会用准备区的旧报告把已完成传送误报为超时。

`0.41.4` 将距离调查的“反向直跑”替换为战斗区内的候选点规划。远离时先选择怪物径向外侧的目标点；1.5 秒没有产生有效接近便判定路线受空气墙阻挡，依次尝试左右切线和反向候选。MHRice Map 14 静态点位用于地图身份与层级约束，运行时实位移负责可达性判定；公开数据未提供的空气墙多边形不再由代码臆造。

`0.41.5` 将空气墙候选切换严格限定在远离模式；距离过远时只执行直接接近，不再因怪物自身移动而错误轮换远离候选。本轮 2400 个有效战斗样本首次在新规划器下捕获 5002 起手（17.54 米）并观察到原生 5001 后继，作为长距钩咬条件的第一份独立距离样本。

`0.41.6` 将自动距离扫描改为可配置的多周期窄带采样。工具可围绕已发现的触发距离反复穿越内外圈，在一次启动中集中收集多个条件招式样本；默认值仍保留通用的 7↔28 米全范围扫描，避免把轰龙的经验阈值写进通用控制器。

`0.42.0` 依据两轮独立自然行为调查，将长距半回转钩咬加入正式关键起手目录。Action 5002 在 11.51 米与 17.54 米起手样本中均由引擎原生续接 Action 5001；产品使用 15±5 米作为观测站位先验，并明确不将其解释为 AI 的通用硬阈值。运行时 UI 合约已自动确认 `查看派生树：5002 → 5001` 和单轮训练门禁。

`0.43.0` 批量验证大师等级左右投石。左投石 Action 5004 在两轮会话中累计 4/4 完整退出，三连耗时 167–173 帧，后继分别为冲锋、漂移连段和大咬，因此作为“独立关键招式”开放，最多 3 轮且不虚构固定派生。右投石 Action 5005 虽能命中，但 900 帧不退出并触发原生重建恢复，继续保持锁定。

`0.44.0-0.45.0` 将训练后继契约从单一 `expected_successor` 扩展为兼容的 `expected_branches[]`。两轮自然调查合计观察到直线冲锋 Action 2 分别进入急停 Action 10（5 次）和漂移 Action 15（2 次），后者再固定回到 Action 2。产品目录新增“条件派生起手”，显示全部已证实候选、未完全解析的条件说明，并记录本轮实际命中分支；强制 Action 的退出结果不参与派生分类。

`0.45.1` 在不部署游戏的前提下补齐证据流水线。每个终态开发探针报告会先归档到本地 `artifacts/probe_reports/`，再允许下一探针覆盖运行时槽位；离线分析器把自然攻击起手、下一攻击候选和怪物包已审核边分开输出，运行频率永远不能自动升级为固定或条件派生。仓库 Lua 门禁同时改用显式模块路径与 PASS 标记识别，确认 25 项中 23 项通过；既有 `test_action_reader.lua` 与 `test_dev_probe_controller.lua` 失败继续单独跟踪，不以外层命令零退出码伪报全绿。

`0.46.0` 建立训练时间轴领域层，在不增加任何游戏写入的前提下，把 Action 起点、Native 判定开闭、受击和结果组织为有界的单轮事件。开启结果追踪时区分受击/无伤；只读模式只允许“观察到受击/未分类”，不因没读到伤害就宣称成功。普通重置清除未完成轮但保留上一完整轮，后续复盘 UI 与输入分类消费同一只读快照。同步修正两个过时测试夹具后，整仓 27 项 Lua 行为测试均以显式 PASS 标记通过。

`0.46.1` 增加可单独关闭的紧凑单轮复盘：只显示招式名、最多两段判定窗口和文字结果，不重复 Response 的详细应对。View 按领域修订号缓存摘要，绘制前使用当前字体实际宽度约束每一行；离线覆盖 640×360 最低内容宽度、1920×1080 完整摘要和缓存失效，真实游戏画面仍等待集中部署验收。

`0.46.2` 修复开发部署白名单遗漏时间轴领域与呈现模块的问题，并把部署测试升级为完整生产文件清单比较。测试会在唯一临时假游戏目录实际执行预检、复制、逐文件 SHA-256 和收据核验，同时确认配置、校准与运行证据不被覆盖；该测试不访问真实游戏安装目录。

`0.47.0` 增加轮询式猎人动作证据层：只读 MotionFsm2 当前节点和 ActStatus 标签，按变化生成最多 128 项的有界事件，并嵌入 `PlayerCombatState.action_state.evidence`。本阶段不安装玩家动作/伤害钩子、不导入无许可证第三方动作哈希、不虚构见切或居合语义；下一次部署后的一个批量会话将同时收集站立、普通攻击、翻滚、见切、纳刀、居合和切书样本。

`0.47.1` 在同一只读 Reader 中加入一次性玩家 FSM 节点目录：每个 FSM 实例最多扫描 4096 项，把当前节点 ID 自动解析为引擎 full name，之后按 ID 查表，不逐帧遍历整棵树。这样下一次实机采样可以优先自动获得可读名称，只有无名称节点才需要后续聚类。

`0.47.2` 修复覆盖报告落后于正式怪物包的问题，并把派生审计拆成 `fixed`、`conditional`、`random`、`observed` 与 `unresolved` 五类。报告可在没有原始实机校准文件时保留既有运行观察、仅刷新静态派生部分；当前确认条件派生 Action 2 不再被遗漏。

`0.47.3` 将一次扫描得到的玩家 FSM 完整节点目录写入有界运行证据，使下一次部署无需逐招操作即可先获得 `ID → 引擎名称` 全集。动作变化只更新内存，并以最多每 60 个采样帧一次的节流策略落盘；脚本正常卸载时强制冲刷，避免高频磁盘写入影响战斗。

`0.47.4` 增加玩家动作证据离线分析器，自动汇总完整目录、实际节点、ActStatus 标签和有向转换，并以太刀术语词根筛出待审核候选。所有转换保持 `observed_runtime_only`，词根匹配不自动升级为“见切/居合成功”，因此能在减少手工枚举的同时保留证据边界。

`0.48.0` 修复实时预测层把所有非固定边压成条件派生的问题。数据包、Model、训练目录和 Overlay 现在端到端保留固定、条件、随机、仅观察与未解析五类；无效的多候选“固定边”降级为未解析，Overlay 分别显示 `fixed/condition/random/observed/unresolved`，不再用笼统 candidate 掩盖确定性差异。

`0.48.1` 增加怪物数据包加载门禁，为后续扩展其他怪物建立最小稳定契约。运行时在数据进入 Model 前验证派生图和训练场景；固定边多后继、条件边无条件、后继或根动作缺失、场景未验证、重复上限非法等情况会拒绝整包并安全降级。Lua 畸形输入测试与真实轰龙 JSON 不变量测试共同覆盖该边界，通用校验器不包含轰龙 Action ID。

`0.48.2` 根据自动实机证据修正玩家动作名称解析：玩家 Motion FSM 在 16.0.2/TDB 71 可读取当前节点 ID，但树枚举数量返回 0。Reader 现优先按当前 ID 调用 `get_node_by_id` 并缓存全名，仅在运行时确实支持时批量枚举；仍保持轮询只读，不安装动作 Hook，不修改装备、动作树或判定帧。

`0.49.0` 增加数据驱动的太刀动作语义层与玩家应对时间轴。见切、特殊纳刀、居合和圆月的公开节点名称先作为 TDB 71 社区候选；精确成功节点优先于动作族前缀，未知节点安全返回未映射。Model 只在语义变化时记录 `player_action`，复盘可显示尝试或成功专用节点，但评分仍不把“无伤”等同于成功反击。全流程不会选择、覆盖或保存玩家装备与替换技。

`0.49.1` 批量验收关键独立起手，并修复猎人死亡后批量动作探针停在准备区的问题。Action 29 正面咬击在两个会话中 6/6 完整进入并退出，现作为最多 5 轮的独立关键起手开放；Action 20、21、26 存在无法可靠退出的样本，Action 24、27、28 未进入写入白名单，均继续只读识别而不进入产品目录。

`0.49.2` 修正指定起手训练只依赖 Action ID 变化判断结束的问题。实机证明行为树已从 `Attack.CheckBite` 返回 `Normal.Search / Wait.NoCombatMode` 后，Action ID 与 Motion 名仍可能残留；训练控制器现在以已观察到的 `Attack.* -> Normal/Wait/Move` 转换确认真实退出，并保留 Action ID 变化作为兼容回退。未知节点或只看到非攻击节点时不会凭 Motion 名臆测完成。自动化产品验收会话 `5c3e72e557fd4f0b9d9366797619a3ea` 已完成正面咬击 5/5，实际路径稳定包含 `Attack.CheckBite.Phase00 -> Phase01 -> Normal.Search.Phase00`。

`0.49.3` 将同一行为树退出语义加入开发批量探针。此前“未在 900 帧内退出”的结论可能同时混入真实卡住与 Action ID 残留两类情况；新报告以 `completion_basis` 明确区分 `action_identity_changed` 与 `behavior_tree_attack_exit`，随后只复测既有白名单候选，且复测结果仍需经过产品入口验收才可开放。

`0.49.4` 根据一次集中批量复测筛选高价值起手。Action 20、21、22、23、26 均通过 `Attack.* -> Normal.Search` 行为树退出确认；产品仅暂存差异化明显的 Action 21“大咬”和 Action 26“右回旋攻击”，各限制为 1 轮并标记产品验收待完成。小咬与普通左右投石因和现有入口同质而延后，避免菜单随 Action 数量无边界膨胀。本版本目前只存在于源码，等待下一次集中部署后在同一任务进程中连续执行两个产品入口验收。

`0.49.5` 收紧太刀实时应对的姿态门禁与排序。交换技书只证明招式已装备：居合拔刀气刃斩必须同时确认特殊纳刀姿态与怪物前摇才显示可用；神威居合缺少可区分的精确姿态节点时保持等待，不从宽泛动作族猜测。攻击判定中优先回避，特殊纳刀姿态优先居合，收招优先兜割/樱花，普通前摇再按资源显示见切、刚·气刃斩或水月。该版本只存在于源码，且不读取或覆盖玩家装备选择；下一次集中部署只做只读状态与显示验收。

`0.49.6` 增加证据驱动的训练结果分类器，并把猎人动作事件放到怪物判定时间轴上的真实采样时刻。明确受击、已验证成功节点、生命追踪无伤、社区成功候选、动作尝试和未分类六类结果使用稳定契约；社区候选不会升级为成功，单纯无伤也明确标注“应对方式待确认”。Model 负责分类，View 只呈现分类结果；后续防御、回避和其他武器只需生成同一事件契约，不在 UI 中追加武器判断。该版本目前只存在于源码，等待下一次集中部署采集真实节点后再升级具体成功映射。

`0.49.7` 将只读 `ActStatus.Guard / Escape / Damage` 转为去重的玩家状态时间轴事件，附带怪物阶段、动画帧和采样时刻。Damage 可作为明确受击证据；Guard/Escape 只分类为防御/回避尝试，生命追踪无伤时也明确标注“成功未确认”，避免把站位避开攻击误判成闪避成功。进入、保持、退出和跨怪物 Action 的生命周期均有离线状态机测试；真实 `Guard` 标签在太刀上是否可用仍等待集中部署采样。

`0.49.8` 为结果分类增加结构化应对时机。受击优先显示相对已确认判定窗的帧差；见切/居合/防御/回避尝试显示采样时的判定前、判定中或判定后帧数。只在应对落到收招阶段时标注“可能偏晚”，前摇期没有武器专属成功窗时不臆测“过早”。这一层只消费既有时间轴事件，不增加 Hook 或游戏写入。

`0.49.9` 修正跨武器动作证据归属。每个玩家动作事件现在携带独立 `player_type`，武器变化会形成新的事件边界；离线分析不会跨武器连接动作转换，太刀知识包遇到盾斧等不匹配类型时关闭语义映射。开发探针报告同时保存训练时间轴快照，使后续一次场景验收即可带回判定窗、玩家状态、受击和结果分类，不再拆成多轮人工验收。

`0.49.10` 将训练 Controller 已验证的 `Attack.* -> Normal/Wait/Move` 行为树退出传给 Model，关闭同一轮判定时间轴并生成结果；残留 Action ID 的后续轮询不会重新打开伪轮次。重复训练再次观察到已请求起手时可显式重开同一 Action 的新轮次，使指定起手完成状态、复盘结果和多轮循环共享同一语义边界。

`0.49.11` 将玩家动作关联从“在轰龙旁逐招手动操作”迁移到自动化开发校准任务。临时 Quest ID `200032002` 使用普通狩猎发布路径，但零怪物生成、零奖励，只在游戏关闭时暂存并在会话结束后删除；正式任务 `200032001` 的前后 SHA-256 保持一致。开发工具复用已校验部署、保留玩家现有太刀与红蓝书配置，使用官方默认键位白名单批量发出直斩、突刺、回避、见切、特殊纳刀与居合尝试。当前已实机确认启动链和 `attack/escape` 节点变化，完整七步仍待一次集中复验，因此不会提前把候选节点升级成“见切/居合成功”。

`0.49.12` 自动分析每次太刀输入校准报告：七步任一缺失、未观察或只有待机节点都会令会话保持失败；完整报告同时标出跨步骤共享节点、单步独占节点和终态相关节点，减少后续人工阅读原始事件。输入关联只生成候选层，不直接修改正式语义映射；见切与居合仍需专用成功节点或判定重叠证据才能升级。

`0.49.13` 第三轮实机报告已达到输入传输 7/7，并在正常退出后删除临时 Quest ID `200032002`，正式陪练任务哈希前后一致。分析仍正确停在 `partial`：实际节点只有普通 `atk_101/102/104/106` 和回避，未命中社区源已知的见切 `atk_147`、特殊纳刀 `atk151.atk_152` 与居合气刃斩 `atk151.atk_155`。`0.49.14` 将验收拆为输入传输与招式节点语义两层，用动作转换信号代替固定延迟；该版已部署，但首次集中运行被人工中止，没有形成新语义结论。

`0.49.15` 增加开发自动化的玩家接管门禁。外部输入桥只在校准开始时聚焦游戏一次，后续失焦立即释放输入并中止，不能循环抢焦点；校准包装器拒绝在游戏已运行时启动，结束后默认不关闭游戏。匹配终态报告的请求文件会自动删除，避免工具进程被中断后留下含混的控制状态。此项不扩大游戏写入，也不改变正式陪练任务。

`0.49.16` 用一次完整启动、18 次动作转换的证据定位并修复气刃键 raw 映射。Capcom Windows 默认 `Mouse Button 4` 对应 Win32 第一个 X 按钮 `XBUTTON1 / 0x0001`，旧代码误发第二个 X 按钮。输入操作现在以具体异常区分 `action_signal_timeout`、`player_takeover` 和 `input_send_failure`；每步失败仍保存从基线开始的实际节点，超时后恢复空闲并继续其他独立步骤，避免一次昂贵启动只得到一个错误字符串。

`0.49.17` 根据 `0.49.16` 的完整复测取消“固定七步适用于所有配装”的假设。测试计划读取当前活动书的五个稳定替换技：只有装备 `special_sheathe_combo` 时才包含特殊纳刀、居合拔刀斩和居合拔刀气刃斩；装备 `sacred_sheathe_combo` 时明确列出三项排除原因。报告保存实际 `expected_step_ids`，分析器以本次计划而非全局常量验收。现有 `input_motion_metadata` 同时增加对已知 `snow.StmInputManager` 类型层级的关键词过滤元数据，不枚举全部 TDB、不调用未知输入方法，用于下一次部署后定位本机真实绑定入口。

`0.49.18` 将输入逆向边界收敛为“游戏语义命令优先”。探针仅枚举四个明确命名的输入枚举和两个明确命名的输入类型，每类最多记录 256 个成员；未知类型输出 `available=false`，不会扩展为全 TDB 扫描。采样结果用于确认 `CommandButton2` 的真实成员与设备值，后续自动校准应依赖语义命令或游戏配置解析，而不是依赖某台电脑的物理鼠标按钮编号。

`0.49.18` 实机报告取得 56 个 `CommandButton2` 成员，包括 `Atk_X`、`Atk_A`、`Atk_R1`、`Escape` 和多种组合命令；同时发现 `TryGet_main_pl_Conf`、`TryGet_sub_pl_Conf`、`TryGet_pad_pl_Conf` 等只读映射候选。完整摘要与原始报告哈希见 `docs/evidence/SEMANTIC_INPUT_CONTRACT_2026-08-28.json`。`0.49.19` 扩展同一个有界元数据探针，枚举映射接口所需的精确参数类型，并沿 `snow.StmPlInputData` 父类型最多追踪八层；每类仍限制 256 个成员，不扫描全 TDB。

`0.49.19` 实机报告完整取得 `PL_INPUT` 105 项、`STATIC_PL_INPUT` 35 项、键鼠键码 159 项、手柄按钮 37 项和鼠标按钮 9 项，全部未触发截断；报告确认 `ACTION_X_ATTACK=13`、`ACTION_A_ATTACK=14`、`ACTION_ESCAPE=8`。证据见 `docs/evidence/CURRENT_BINDING_ENUM_CONTRACT_2026-08-28.json`。下一门禁是读取 `TryGet_*` 参数名称与 `is_by_ref` 元数据；在确认参数方向前禁止凭 `TryGet` 命名猜测调用方式。

`0.49.20` 扩展方法元数据契约，不改变训练业务或输入执行器。每个参数以 `{index,name,type,is_by_ref}` 独立记录，并保留旧 `param_types` 字段供已有证据工具兼容。真实探针只读取 TDB 元数据；只有 main/sub/pad 三个候选方法的参数方向和返回类型一致且可解释时，下一版本才允许实现受限的当前绑定读取器。

`0.49.20` 实机确认 `TryGet_main_pl_Conf`、`TryGet_sub_pl_Conf`、`TryGet_pad_pl_Conf` 和 `TryGet_pad_pl_Static` 的两个参数均为普通输入参数，返回 `System.Boolean`；它们是绑定匹配谓词而不是带输出缓冲区的 getter。`0.49.21` 因此只扫描四个明确逻辑动作，每个映射命中首个候选即停止；键鼠和手柄候选总规模保持在已验证枚举内，超过 2048 次立即显式截断。

`0.49.21` 首轮真实调用共发起 12 次只读谓词查询，12 次均在每条路径的第一个 `None/NONE=0` 候选抛出异常；脚本捕获异常、任务继续、游戏未崩溃。`0.49.22` 将哨兵和 `*Mask` 从物理键候选剔除，并将孤立异常记录为有界诊断后继续；累计 32 次异常时立即停止，禁止用无界容错掩盖错误调用契约。

`0.49.22` 的第二轮实机调用排除零值后仍以 43/43 异常结束，游戏未崩溃且玩法请求/写入均为零。由此停止继续猜测 `TryGet_*` 的 Lua 调用约定；`0.49.23` 转而从四个已知配置字段取得真实字典对象，只提取精确方法签名。下一门禁是证明字段 owner、对象类型和 `get_Item(System.Int32)` 返回类型均一致，再在独立版本中读取四个逻辑动作，避免把元数据发现与方法调用合并成一次高风险变更。

`0.49.23` 实机确认 main/sub 键鼠字典为静态字段，pad/player-static 字典为实例字段；四者声明类型与真实对象类型一致。键鼠 `get_Item(Int32)` 返回 `InGameMouseKeyBoardKey`，手柄返回 `snow.Pad.Button`，探针请求/写入为零。`0.49.24` 只对已知逻辑 ID `8/13/14/15` 执行 `ContainsKey` 后的 `get_Item`，任何缺键、方法异常或不可数值化结果都单独失败关闭，不影响训练业务。契约证据见 `docs/evidence/BINDING_DICTIONARY_CONTRACT_2026-08-28.json`。

`0.49.24` 实机以 23 次只读字典调用完成四项解析，调用失败和数值解析失败均为零：闪避为 `Space/RD`，X 为 `MOUSE_L/RU`，A 为 `MOUSE_R/RR`，武器特殊键主键为 `MOUSE_EX1`；四项副键均为 `None`。武器特殊键在玩家手柄字典中不存在独立条目，因此明确输出 `key_unavailable`，不推断组合按钮。下一步只消费已解析项生成校准输入计划和冲突门禁；证据见 `docs/evidence/CURRENT_BINDING_READ_2026-08-28.json`。

`0.49.25` 已在源码中完成运行时键位校准计划：语义动作定义不依赖某台电脑的侧键，预检报告同时提供动作节点与当前绑定，执行器仅解析明确支持的键鼠值，并把实际角色映射写入会话报告。快捷键冲突门禁没有与本批强行合并，因为产品侧读取 `via.hid.GamePadButton`，游戏配置侧读取 `snow.Pad.Button`；在取得可靠跨枚举映射前保持不可用比比较 raw 值更安全。下一门禁是集中实机证明当前 `MOUSE_EX1` 计划能命中见切节点，并保留失败步骤的传输/语义证据。

为控制实机等待成本，可信度升级采用分级门槛：按键—动作节点和带结果事件的武器应对各 2 次一致；固定派生与判定窗各 3 次；条件派生每个关键条件分支各 2 次。结果冲突才追加一次，不再默认执行五轮。动作节点关联只证明招式执行，不能替代见切/居合成功证据。

`0.49.25` 的 `mouse_event` 与批量 `SendInput` 两轮实机结果一致：当前绑定读取零失败，直斩/突刺/翻滚通过，见切仍只出现普通直斩。由此结束 Windows 物理侧键注入调查；下一批先为 `snow.StmPlayerInput` / `CommandButton2` 建立精确方法、字段、更新时序和释放契约，只有只读门禁完整后才做一次语义位写入实验。REFramework 的 RE7/RE8 VR 脚本可作为“在游戏输入更新点合并按钮位”的结构参考，但 MHR 既有三轮通用 HID 轴/按钮实验均无位移，不能直接照搬为已验证实现。

`0.49.26` 已在源码中完成只读语义输入元数据契约。它有界检查 `snow.StmInputManager`、`snow.StmPlayerInput`、`snow.player.PlayerInput`、`snow.StmInputManager.InputUI`，并单独列出 `CommandButton2` 与 `getOn/getTrg/getRel/getDelay/isOn/isTrg/isRel/isDelay` 查询签名。离线假 SDK 测试证明契约不会调用这些方法；本版暂不部署，因此所有运行时类型和单例结果仍保持候选。下一门禁是一次集中只读采集，随后才决定是否存在可成对实现“按下/释放”的更新入口；若不存在则停止游戏内注入路线，不再增加猜测轮次。

`0.49.26` 随后完成一次集中只读验收：四个目标类型全部存在，`CommandButton2` 共 56 项；`snow.StmInputManager` 是唯一可直接取得的实例，具有四个位集 getter 和 `update/updateInGameFrame`，而带 `setButton/clearButton` 的 `snow.StmPlayerInput` 不是受管单例。该结果禁止直接调用后者。`0.49.27` 的四个 Manager getter 随后也以 4/4、零失败完成真实验收，但返回位集的精确类型无公开成员；因此 `0.49.28` 只检查继承层级和当前猎人中的实际输入对象，不写入。两层证据分别见 `docs/evidence/SEMANTIC_INPUT_METADATA_ACCEPTANCE_2026-08-28.json` 与 `docs/evidence/SEMANTIC_BITSET_ACCEPTANCE_2026-08-28.json`。

当前主线已由“逐项强制 Action”收敛为“指定起手、原生续接、实时派生训练”。首个用户入口“执行：咆哮”已在 `0.33.0-specified-move-training` 由用户实机确认，`0.34.0-repeat-training` 又通过真实 Controller 自动完成 3/3 循环；小咬扩充仅完成 1/3 后出现 Action 不退出，证明一次注入成功不能代表可重复训练，入口已撤回。下一批先建立稳定空闲入口和 `native_branch` 路径追踪，再选择一个具有明确派生的根动作验证，后续不逐个强塞 Action。直接局内重置仍只接受引擎原生重建/重置接口，不恢复已证明会崩溃的 Transform 写回路径。古塔准备区到战斗区已采用坐标导航、一次性交互和战斗层判定自动完成，开发探针可在单项异常后通过 F7 恢复并继续采样。

`0.34.3` 的自动批量探针进一步否定了“由静态 Action 边直接挑根动作”的捷径：Action 15 和 18 的真实退出均为 category 1/action 8，而不是静态图的 category 4/action 2；Action 16 进入 `TigDashLoop_New` 后未在 900 帧内退出并被自动恢复。因此下一步不扩大 Action ID 白名单，改为寻找能保留 Think/Combo/FSM 上下文的高层入口；在找到安全写入口前，运行时自然选中根动作的只读追踪可先用于验证派生树。

`0.36.0` 的 3600 帧自然行为调查记录了 20 个 FSM 节点、31 条边，并观察到 `BiteHookHalfTurnStartShortRange (Action 5000) -> BiteHookHalfTurnAttackNormal (Action 5001)`。随后仅注入一次 5000、完全不写后继的对照实验却转入 `Move.Dash`，证明 Action 请求会丢失原生 Think/FSM 分支上下文。该候选不进入用户菜单；下一阶段限定为寻找高层 Think/FSM 激活入口或可自动构造的原生条件诱导，独立单招路径与原生派生路径从此分开验收。

`0.37.0-0.37.4` 对 `combo_001` 执行了 5 个自动恢复的高层入口实验，分别否定活动父状态直设、单独 `nextJumpThinkData`、追加 `startActionTable`、信任返回对象以及从活动栈解析新子树。全部由 F7 原生重建恢复且未崩溃。下一实现不再构造内部 ThinkInfoData，改为自动控制诱导条件并筛选 AI 自然选中的 5000/5002；只有选中目标根动作后才进入训练计分和原生 5001 续接观察。

`0.38.0` 增加轻量玩家—怪物空间采样和外部正常移动闭环，在单轮 7200 帧的“近距→远距→近距”调查中自动诱导出 3 次 `BiteHookHalfTurnStartShortRange (5000) -> BiteHookHalfTurnAttackNormal (5001)`。5000 起手距离为 4.87–8.52 米，5001 均由引擎自然续接且未注入。下一阶段以该证据实现“指定起手”的条件诱导控制器：自动进入目标距离带、等待 AI 自选目标根、命中后停止移动并交给原生派生；距离范围先作为轰龙 Profile 的观测先验，不宣称通用硬阈值。

`0.39.0` 完成白名单条件诱导状态机及三轮自动实机验收。三轮均在正常移动进入 7±2 米目标带后自然选中 Action 5000，随后立即释放移动，并分别在 79、85、77 帧后观察到引擎续接 Action 5001；无怪物行为写入、无崩溃。开发探针已经验证业务状态契约，下一步必须解决产品运行时的移动适配：优先寻找 REFramework/游戏内原生输入入口；若不存在安全入口，则产品 UI 降级为明确的距离引导，由玩家正常移动，状态机仍自动识别根招与派生。不得把 Windows 外部按键脚本作为最终 Mod 依赖。

`0.39.1-0.39.4` 完成《崛起》HID 输入链调查。只读探针确认 `via.hid.GamePadDevice.set_AxisL(via.vec2)` 存在；随后三次有界实验分别验证 UpdateBehavior 前写轴、REFramework VR 同款 UpdateHID 后写轴，以及 AxisL 加 EmuLup 活动位，三次位移均为 0，且全部安全恢复零轴。由此否定把通用 `via.hid.GamePadDevice` setter 当作产品移动适配器。MVP 改为产品内实时距离引导 + 自动根招/派生识别；开发验收继续复用外部正常输入自动化。完全自动的产品移动若继续推进，应作为独立 C++/XInput 插件或在找到已验证的 MHR `snow.StmPlayerInput` 入口后实施，不阻塞出招提示、派生树和陪练闭环。

`0.40.0` 将上述验证结果接入正式训练 Controller。训练场景现在显式区分 `forced_single` 与 `natural_condition`：后者读取 Profile 中的目标距离带，在 Overlay 中给出“接近/远离/等待起手”，观察到根动作后自动锁定训练阶段，只在引擎自然进入已验证后继时计为完成。首个场景为 7±2 米条件下的 Action `5000 -> 5001`；实现不依赖手柄、不发送外部按键，也不写怪物 Action/Think/FSM。

`0.40.1-0.40.2` 为连续无人值守验收增加场景生命周期门禁。一次 3 连验收在原生区域传送期间触发游戏本体 `c0000005`，另一次在进入战斗区后退出于 AMD 驱动 `amdxc64.dll`。Runtime 现改为先刷新 actor 引用，仅在猎人与目标怪物垂直层差不超过 50 米时采集武器/技能状态；跨层与加载期清除瞬时战斗状态。自动移动遇到异常高度会立即释放。曾尝试的开发期生命维持因未形成独立安全证据已撤回；不再用持续追怪的无人值守脚本冒充 3/5 连产品稳定性，正式产品仍无生命写入。

`0.40.3` 将重复次数纳入场景证据契约。咆哮保留已支持的多轮选择；短距半回转钩咬在 3/5 连门禁通过前由数据声明 `max_verified_repeats=1`。菜单仍可保留全局次数偏好，但开始按钮显示实际有效轮数并解释降级原因，Controller 再次强制钳制，避免 UI 绕过或旧配置把候选能力伪装成已验证能力。

`0.40.4` 将 ImGui 实际消费的按钮标签、有效轮数和门禁文案提取为同一呈现契约，并增加只读运行时快照探针。实机探针在未加载存档、未进入任务、零输入和零玩法写入的条件下 1 帧完成：请求 5 次时咆哮输出 `×5`，短距半回转钩咬输出 `×1` 并携带稳定性门禁说明。该类确定性 UI 验收不再转交用户手工操作。

开发验收器已能自动启动游戏、选择首个存档、进入离线大厅、直接接取固定陪练任务、等待任务稳定、执行一次 F7 完整重开并核对重新进入。任务接取阶段不再依赖窗口焦点或菜单按键；外部按键桥仅用于标题页和存档页。环境生物自动验收默认仅做只读观察，主动生成候选因当前运行时容器不兼容且曾触发原生崩溃而保持关闭。

`tools/audit_coaching_coverage.py` 将怪物静态数据包与实机校准证据合并审计，统一列出已见招式的名称、应对、可靠阶段和派生缺口。当前轰龙报告保存在 `docs/evidence/TIGREX_COACHING_COVERAGE.json`：18 个已见攻击均有名称和应对，14 个已有重复或确认的判定阶段；当前数据包包含 5 个固定派生来源和 1 个条件派生来源。固定、条件、随机、仅观察与未解析类别分别统计，禁止用“非固定”混称条件派生。

古塔 Map 14 的公开地图坐标来自 MHRice，原生 `QuestAreaMovePopMarker` 的精确位置由运行时焦点事件交叉确认并固化为静态默认数据；新安装无需再做首次盲扫。区域枚举在该地图不一致，因此战斗区门禁使用猎人与目标怪物的实时垂直层差。完整坐标、传送和强制动作证据见 `docs/evidence/ARENA_NAVIGATION_AND_FORCED_ACTION_2026-08-22.json`。

## 长期不变量

- Steam PC、单人训练；多人时写入能力全部禁用。
- 不修改存档、奖励、正式任务装备或联机数据。
- 静态名称和 AI 结构优先从成熟数据源离线导入；游戏运行时只读取必须实时变化的状态。
- 未知、随机、条件和固定派生必须显式区分。
- 每只怪物的数据独立版本化；通用代码不写死轰龙 Action ID。
- 所有游戏状态写入都必须有退出、异常和重开恢复路径。

## 明确保留但不开发

### 仅怪物减速实验模式

当前决策：`不开发`。以下内容仅保存未来重新评估时必须满足的完整性门禁，不代表已经排期：

- 与全局子弹时间分开配置，不互相覆盖。
- 全局子弹时间用于保持猎人与怪物的真实速度比例，只延长现实思考时间。
- 仅怪物减速用于观察前摇、判定和派生；必须醒目标注“不代表正常速度下的真实应对窗口”。
- 怪物动画、AI Action/FSM、根运动、物理位移、转向、攻击判定、怪物投射物和派生计时必须使用同一倍率。
- 任一子系统无法同步时，该怪物或该招式保持不可用，不允许以视觉减速冒充完整实现。
- 退出任务、切换怪物、进入多人、脚本异常或关闭功能时，所有倍率和临时状态必须恢复。

### 真正的视频或世界状态回放

当前决策：`不开发`。

- 正式替代方案是“判定与输入事件时间轴”，记录 Action/FSM、Motion 帧、判定窗口、玩家关键输入、结果和派生转换。
- 不自行录制长视频，不把视频文件管理、编码和回放 UI 纳入 Mod 核心。
- 不通过批量写回 Transform、生命、AI、碰撞体、投射物、环境生物或任务对象模拟世界快照。
- 只有发现游戏或 RE Engine 已存在可调用的原生 Replay/SaveState 生命周期，并证明能完整重建上述状态、隔离正式存档且连续运行无崩溃时，才允许重新立项。
